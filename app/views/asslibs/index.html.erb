<p id="notice"><%= notice %></p>

<h1>Asslibs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Type</th>
      <th>Active</th>
      <th>Currency</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @asslibs.each do |asslib| %>
      <tr>
        <td><%= asslib.name %></td>
        <td><%= asslib.description %></td>
        <td><%= asslib.altype %></td>
        <td><%= asslib.active %></td>
        <td><%= asslib.currency %></td>
        <td><%= asslib.user_id %></td>
        <td><%= link_to 'Show', asslib %></td>
        <td><%= link_to 'Edit', edit_asslib_path(asslib) %></td>
        <td><%= link_to 'Destroy', asslib, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Asslib', new_asslib_path %>
